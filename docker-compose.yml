version: '3.8'

services:
  redis:
    image: redislabs/redismod
    ports:
      - '6379:6379'

  web:
    build: .
    stop_signal: SIGINT
    ports:
      - '4000:80'
    volumes:
      - ./app:/app
    depends_on:
      - redis
    networks:
      - my-network

networks:
  my-network:
    driver: bridge
